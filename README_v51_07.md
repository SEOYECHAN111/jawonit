# 자원잇다 v51.08 버튼/관리자/푸터 수정본

- v50.20 기본구조 유지
- 버튼 클릭 오류 방지 패치
- 관리자 대시보드 재구성
- 푸터 사업자 정보 정리
- Windows 안전 ZIP 구성

실행:
```powershell
python -m pip install -r requirements.txt
python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

관리자: admin / 1234
